{
  "config": {
    "step": {
      "user": {
        "title": "Setup Codes",
        "description": "Keeps pairing codes for Matter, HomeKit, and Z-Wave devices, including devices that have left Home Assistant."
      }
    },
    "abort": {
      "already_configured": "Setup Codes is already configured.",
      "single_instance_allowed": "Setup Codes is already configured."
    }
  }
}
